# Dependencias lean para Hugging Face Spaces (Docker, CPU gratis).
# Igual que apps/api/requirements.txt pero SIN sentence-transformers:
# ese paquete arrastra torch (~2GB) y en el Space usamos el embedder
# hash offline (RAG_OFFLINE=True), que no necesita torch.
# El resto se mantiene para no divergir del entorno local.
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.30
asyncpg>=0.29.0
geoalchemy2>=0.15.0
shapely>=2.0.4
httpx>=0.27.0
redis>=5.0.4
python-dotenv>=1.0.1
pytz>=2024.1
pgvector>=0.3.0
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
apscheduler>=3.10.0
python-multipart>=0.0.18
